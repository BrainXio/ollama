# This file provides example environment variables for configuring the Ollama server.
# Copy to .env, uncomment (remove the #), and set values as needed.
# Variables are ordered by usage frequency, with commonly modified ones first.

# OLLAMA_PORT
# Specifies the host port for Ollama services, mapped to container port 11434.
# Example: "127.0.0.1:11434" binds to localhost; "11434" exposes publicly.
# Default: 127.0.0.1:11434 (localhost binding).
#OLLAMA_PORT=127.0.0.1:11434

# OLLAMA_VERSION
# Specifies the Ollama image version for CPU and NVIDIA GPU services.
# Example: "0.3.12" uses a specific version; "latest" uses the newest release.
# Default: latest.
#OLLAMA_VERSION=latest

# OLLAMA_VOLUME
# Specifies the storage for Ollama models, either a local path or Docker volume.
# Example: "./ollama_data" for local directory; "ollama-data" for Docker volume.
# Default: ollama-data (Docker volume).
#OLLAMA_VOLUME=ollama-data

# TSAUTHKEY_PATH
# Specifies the path to the Tailscale authentication key file.
# Example: "~/.secrets/ollama-tsauthkey.key" points to the auth key.
# Default: tsauthkey (local file).
#TSAUTHKEY_PATH=tsauthkey

# OLLAMA_NAME_SUFFIX
# Adds a suffix to the Tailscale hostname.
# Example: "-small" sets hostname to "ollama-small".
# Default: None (no suffix).
#OLLAMA_NAME_SUFFIX=-small

# TAILSCALE_VERSION
# Specifies the Tailscale image version.
# Example: "1.74.0" uses a specific version; "stable" uses the latest stable release.
# Default: stable.
#TAILSCALE_VERSION=stable

# OLLAMA_NETWORK_NAME
# Specifies the name of the Docker network for Ollama and Traefik.
# Example: "ollama-net" creates a network with this name.
# Default: ollama-net.
#OLLAMA_NETWORK_NAME=ollama-net

# OLLAMA_NETWORK_EXTERNAL
# Indicates if the Docker network is external (already created).
# Example: "true" uses an existing network; "false" creates a new one.
# Default: false.
#OLLAMA_NETWORK_EXTERNAL=false

# OLLAMA_ENABLE_TRAEFIK
# Enables Traefik for HTTPS routing to Ollama via the specified domain.
# Example: "true" enables Traefik; "false" disables it.
# Default: false.
#OLLAMA_ENABLE_TRAEFIK=false

# DOMAIN
# Specifies the domain for Traefik routing.
# Example: "yourdomain.com" routes to ollama.yourdomain.com.
# Default: None (required if OLLAMA_ENABLE_TRAEFIK=true).
#DOMAIN=yourdomain.com

# IP_WHITELIST
# Restricts Traefik access to specified IP ranges.
# Example: "192.168.1.0/24" allows a subnet; "0.0.0.0/0" allows all.
# Default: 0.0.0.0/0 (all IPs).
#IP_WHITELIST=0.0.0.0/0

# OLLAMA_FLASH_ATTENTION
# Enables memory-efficient processing for Ollama models.
# Example: "1" enables Flash Attention; "0" disables it.
# Default: 1 (enabled).
#OLLAMA_FLASH_ATTENTION=1

# OLLAMA_KV_CACHE_TYPE
# Sets the data format for memory optimization when Flash Attention is enabled.
# Example: "q8_0" optimizes memory usage.
# Default: q8_0.
#OLLAMA_KV_CACHE_TYPE=q8_0

# OLLAMA_MAX_LOADED_MODELS
# Limits the number of models loaded in memory simultaneously.
# Example: "2" allows two models to be loaded.
# Default: 2.
#OLLAMA_MAX_LOADED_MODELS=2

# OLLAMA_BASE_MODEL
# Specifies the base model pulled by the initialization container.
# Example: "qwen2.5:7b-instruct-q4_K_M" for a quantized Qwen model.
# Default: smollm2.
#OLLAMA_BASE_MODEL=smollm2

# OLLAMA_EMBEDDING_MODEL
# Specifies the embedding model pulled by the initialization container.
# Example: "nomic-embed-text" for text embedding tasks.
# Default: smollm2.
#OLLAMA_EMBEDDING_MODEL=smollm2

# OLLAMA_THINKING_MODEL
# Specifies the thinking model pulled by the initialization container.
# Example: "llama3.2" for a specific model.
# Default: smollm2.
#OLLAMA_THINKING_MODEL=smollm2

# OLLAMA_TOOLS_MODEL
# Specifies the tools model pulled by the initialization container.
# Example: "codegrok" for a coding-focused model.
# Default: smollm2.
#OLLAMA_TOOLS_MODEL=smollm2

# OLLAMA_VISION_MODEL
# Specifies the vision model pulled by the initialization container.
# Example: "llava" for a vision-capable model.
# Default: smollm2.
#OLLAMA_VISION_MODEL=smollm2